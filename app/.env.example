# Local Supabase config
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# OAuth providers like Google and Discord are enabled in the Supabase dashboard.
# Use http://localhost:3000/auth/callback for local auth redirects.
# Use https://feh-barracks-manager.vercel.app/auth/callback for production auth redirects.

# Optional override for local data path
# DB_ROOT=
